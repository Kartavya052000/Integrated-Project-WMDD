// function autocomplete(input) {
        //     const apiUrl = `https://nominatim.openstreetmap.org/search?format=json&q=${encodeURIComponent(input)}`;

        //     fetch(apiUrl)
        //         .then(response => response.json())
        //         .then(data => {
        //             // Extract relevant information from the response, e.g., display suggestions to users.
        //             console.log('Autocomplete Suggestions:', data);

        //             // For simplicity, you can just select the first result and set it as the input value
        //             if (data.length > 0) {
        //                 locationInput.value = data[0].display_name;
        //             }
        //         })
        //         .catch(error => console.error('Error fetching autocomplete data:', error));
        // }

